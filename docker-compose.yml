# Local: builds from source. Published on a host port so you can open it.
#   docker compose up -d --build   → http://localhost:8089
services:
  bd-tax-site:
    build: .
    container_name: bd-tax-calculator-site
    restart: unless-stopped
    ports:
      - "8089:80"
